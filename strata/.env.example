# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/strata

# Embeddings (optional)
ENABLE_EMBEDDINGS=false
OPENAI_API_KEY=

# Worker settings
WORKER_POLL_INTERVAL_SECONDS=2.0
WORKER_MAX_ATTEMPTS=3

# Chunking
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
